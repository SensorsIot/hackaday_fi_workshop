## bootup
ch0: reset
ch1: trigger pin pulled HIGH in firmware

## power_trace
stm8s003 power trace
trigger: rst, rising edge
sample rate: 169600000 Hz

## dump_firmware.py
firmware exctraction script - adjust `start`, `stop` and `pattern` parameters

install dependencies first:
```
python -m pip install -r requirements.txt
```
